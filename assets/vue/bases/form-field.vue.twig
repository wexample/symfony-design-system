{%- extends '@WexampleSymfonyDesignSystemBundle/vue/bases/vue.vue.twig' -%}

{%- block field_label -%}
    <label v-if="hasLabel" class="form--label" :for="resolvedId">[[ resolveLabel(label) ]]</label>
{%- endblock -%}

{%- block field_control -%}{%- endblock -%}

{%- block field_errors -%}
    <div class="form--field-errors" v-if="hasErrors">
        <ul>
            <li v-for="(error, index) in resolvedErrors" :key="index">
                <template v-if="hasErrorTranslationPrefix">
                    [[ trans(errorTranslationPrefix + error) ]]
                </template>
                <template v-else>
                    [[ error ]]
                </template>
            </li>
        </ul>
    </div>
{%- endblock -%}

{%- block field_help -%}
    <div class="form--help" v-if="help">[[ help ]]</div>
{%- endblock -%}

{%- block vue_body -%}
    <div class="form--group" :class="{ 'has-error': hasErrors, 'is-disabled': controllerDisabled }">
        {{ block('field_label') }}
        {{ block('field_control') }}
        {{ block('field_errors') }}
        {{ block('field_help') }}
    </div>
{%- endblock -%}
