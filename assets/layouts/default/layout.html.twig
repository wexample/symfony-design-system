{%- extends adaptive_response_rendering_base_path() -%}

{%- set layout_path = layout_path|default(_self) -%}

{%- block layout_config -%}
    {{ render_pass.setUsage('color_scheme', 'dark') }}
    {{ parent() }}
{%- endblock -%}

{%- block layout -%}
    {{ parent() }}
    <div class="page-overlay"></div>
    <div class="page-overlay-full"></div>
{%- endblock -%}
