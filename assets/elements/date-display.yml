# Declares `date` to the design system registry.
# Seeded from a scan of the assets; kept by hand from here on.
# formats, per format — whose move it is next:
#   true            it is there, nothing is owed
#   todo — a note   it is wanted and missing, someone should build it
#   a sentence      it does without on purpose, and this is why
#   left out        nobody has ruled yet, and the check will name it
description: "The date a page shows, with the full one in its tooltip. Named for what it does rather than for what it holds: `date` would give a vue identifier shadowing javascript's `Date`, and the directory, the file and the component must read the same everywhere."
formats:
  style: true
  template: "todo — nothing renders it server-side"
  vue: true
  script: "it shows and does nothing"
