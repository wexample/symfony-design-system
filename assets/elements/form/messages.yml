# Declares `form/messages` to the design system registry.
# Seeded from a scan of the assets; kept by hand from here on.
# formats, per format — whose move it is next:
#   true            it is there, nothing is owed
#   todo — a note   it is wanted and missing, someone should build it
#   a sentence      it does without on purpose, and this is why
#   left out        nobody has ruled yet, and the check will name it
description: null
formats:
  style: true
  template: "a style the field components share, not a component: it belongs in css/mixins and should move there"
  vue: "a style the field components share, not a component"
  script: "a style the field components share, not a component"
