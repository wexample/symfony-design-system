# Declares `timeline` to the design system registry.
# Seeded from a scan of the assets; kept by hand from here on.
# formats, per format — whose move it is next:
#   true            it is there, nothing is owed
#   todo — a note   it is wanted and missing, someone should build it
#   a sentence      it does without on purpose, and this is why
#   left out        nobody has ruled yet, and the check will name it
description: Things one under the other on markers joined by a line — steps of a procedure, events of a history — each holding whatever it needs.
formats:
  style: true
  template: true
  vue: true
  script: "it lays things out, and a layout needs nothing to run"
  schema: todo
  ai: todo
  react: todo
  flutter: todo
