# Declares `menu-collapsible-panel` to the design system registry.
# Seeded from a scan of the assets; kept by hand from here on.
# formats, per format — whose move it is next:
#   true            it is there, nothing is owed
#   todo — a note   it is wanted and missing, someone should build it
#   a sentence      it does without on purpose, and this is why
#   left out        nobody has ruled yet, and the check will name it
description: null
formats:
  template: true
  script: true
  style: "wears the style of gutters and adds none of its own"
  vue: "todo — a chantier of its own: the twig side is an empty placeholder binding to gutters the layout wrote, so a vue one has to own markup it does not own today"
  schema: todo
  ai: todo
  react: todo
  flutter: todo
