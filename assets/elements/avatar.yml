# Declares `avatar` to the design system registry.
# Seeded from a scan of the assets; kept by hand from here on.
# formats, per format — whose move it is next:
#   true            it is there, nothing is owed
#   todo — a note   it is wanted and missing, someone should build it
#   a sentence      it does without on purpose, and this is why
#   left out        nobody has ruled yet, and the check will name it
description: "A person or a thing, shown small and standing for itself."
formats:
  style: true
  template: todo — nothing renders an avatar today; the entity bar draws the markup itself
  vue: todo — a bar drawn client-side cannot reach a twig component
  script: an avatar shows and does nothing
  schema: todo
  ai: todo
  react: todo
  flutter: todo
