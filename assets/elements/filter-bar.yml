# Declares `filter-bar` to the design system registry.
# Seeded from a scan of the assets; kept by hand from here on.
# formats, per format — whose move it is next:
#   true            it is there, nothing is owed
#   todo — a note   it is wanted and missing, someone should build it
#   a sentence      it does without on purpose, and this is why
#   left out        nobody has ruled yet, and the check will name it
description: "A row of filters, one discreet menu each, saying what it narrows to once it does. It holds no rows: in vue it produces a value for an api or a table, on the server each option is a link changing the page's query."
formats:
  style: true
  template: true
  vue: true
  script: "its options are links on the server; the vue twin holds the value"
  schema: todo
  ai: todo
  react: todo
  flutter: todo
