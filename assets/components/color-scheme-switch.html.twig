<button class="color-scheme-switch" aria-label="{{ label | default('Toggle color scheme') }}">
    <span class="color-scheme-switch--icon color-scheme-switch--light">{{ icon('ph:bold/sun') }}</span>
    <span class="color-scheme-switch--icon color-scheme-switch--dark">{{ icon('ph:bold/moon') }}</span>
</button>
